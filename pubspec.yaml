name: rgb_tape
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.2.2
  shared_preferences: ^2.3.3
  flutter_colorpicker: ^1.1.0
  flutter_secure_storage: ^9.2.4

  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  provider: ^6.1.2
  speech_to_text: ^7.0.0
  audioplayers: ^6.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true
  generate: true
  assets:
    - assets/arabic_song.mp3
